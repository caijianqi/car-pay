# 后端 API 地址配置
#
# 开发环境：留空即可，请求走相对路径，由 vite.config.js 的 proxy 转发到真实后端。
# 生产环境（GitHub Pages 等静态托管）：必须配置真实后端地址，否则请求会打到静态服务器。
#
# 示例：
# VITE_API_BASE=https://parking.yilufa.net:10889
#
# 注意事项：
# 1. 后端必须开启 CORS，允许 https://caijianqi.github.io 跨域访问
# 2. 后端需提供 HTTPS 入口（GitHub Pages 是 HTTPS，调 HTTP 后端会被浏览器拦截为混合内容）
# 3. 若后端不支持 CORS/HTTPS，纯静态部署无法调通接口，建议改为部署到后端同域
VITE_API_BASE=
